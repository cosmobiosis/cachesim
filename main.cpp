#include <iostream>
#include <string>
#include "cache.h"

int main(int argc, char* argv[])
{
  // if (argc < 2) {
  //   std::cout << "Usage: " << argv[0] << "param1" << std::endl;
  //   return 1;
  // }
  // const int param1 = std::stoi(argv[1]);
  // CacheConfig* config = new CacheConfig();
  Memory* memory = new Memory(nullptr);
  return 0;
}
